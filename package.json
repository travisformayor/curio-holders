{
  "name": "curio-holders",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ curio-holders",
    "create-local": "graph create --node http://localhost:8020/ curio-holders",
    "remove-local": "graph remove --node http://localhost:8020/ curio-holders",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 curio-holders"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
